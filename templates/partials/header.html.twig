<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">belle éclat</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
    
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('category_hommes') }}">Hommes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('category_femmes') }}">Femmes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('category_enfants_bebes') }}">Enfants et Bébés</a>
                    </li>
                    <ul>
                {# <li><a href="{{ path('product_homme') }}">Homme</a></li>
               <li><a href="{{ path('product_femme') }}">Femme</a></li>
               <li><a href="{{ path('product_enfants_bebes') }}">Enfants et Bébés</a></li> #}
                   </ul>  
                </ul>
            </div>
        </div>
    </nav>
</header>
